'use client';

import {
  RotateCcw,
  Copy,
  Download,
  Loader2,
} from 'lucide-react';

export const ArrowsCounterClockwise = RotateCcw;
export const DownloadSimple = Download;
export const SpinnerGap = Loader2;
export { Copy };
